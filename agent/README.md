# maid Agent package

composed of `loop`ts (the core loop logic), `index.ts` where the api routes resides and `/skills` & `tools` used to interact with external sources (filesystem, DBs, APIs and so on).